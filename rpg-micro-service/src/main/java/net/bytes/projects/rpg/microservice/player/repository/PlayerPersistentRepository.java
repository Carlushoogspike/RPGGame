package net.bytes.projects.rpg.microservice.player.repository;

import jakarta.inject.Singleton;

@Singleton
public class PlayerPersistentRepository {


/*
    public CompletableFuture<?> findPlayer(FetchFilters filters, String value) {
        Bson filter = MongoFilter.createFilter(filters, value);
        return filter == null ? CompletableFuture.completedFuture(null) :
                CompletableFuture.supplyAsync(() -> collection.find(filter).first(), service);
    }
*/

}
